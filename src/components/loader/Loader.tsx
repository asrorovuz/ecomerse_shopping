import { loader } from "../icon"
import "./index.scss"

const Loader = () => {
  return (
    <div className="loader">
        <img src={loader} alt="loader icon" />
    </div>
  )
}

export default Loader